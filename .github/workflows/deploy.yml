name: Deploy ASP.NET Core API to AWS Elastic Beanstalk

on:
  push:
    branches:
      - common-devlopement
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Publish
        run: dotnet publish -c Release -o publish

      - name: Zip package
        run: |
          cd publish
          zip -r ../app.zip .
          cd ..

      - name: Configure AWS
        run: |
          echo "AWS_REGION=${{ secrets.AWS_REGION }}" >> $GITHUB_ENV
          echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> $GITHUB_ENV
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> $GITHUB_ENV

      - name: Ensure S3 bucket exists
        run: |
          if [ -z "${{ secrets.AWS_ACCOUNT_ID }}" ]; then
            echo "❌ ERROR: AWS_ACCOUNT_ID secret is missing!"
            exit 1
          fi

          APP_NAME="Sample-AUTH-API"
          APP_NAME_LOWER=$(echo "$APP_NAME" | tr '[:upper:]' '[:lower:]')
          BUCKET_NAME="${APP_NAME_LOWER}-${{ secrets.AWS_ACCOUNT_ID }}"

          echo "Using bucket: $BUCKET_NAME"

          aws s3api head-bucket --bucket "$BUCKET_NAME" 2>/dev/null || \
            aws s3 mb "s3://$BUCKET_NAME" --region "$AWS_REGION"

          echo "BUCKET_NAME=$BUCKET_NAME" >> $GITHUB_ENV

      - name: Upload to S3
        run: |
          VERSION_LABEL="v${GITHUB_RUN_NUMBER}"
          aws s3 cp app.zip "s3://$BUCKET_NAME/$VERSION_LABEL.zip"
          echo "VERSION_LABEL=$VERSION_LABEL" >> $GITHUB_ENV

      - name: Ensure EB application
        run: |
          APP_NAME="Sample-AUTH-API"

          if ! aws elasticbeanstalk describe-applications \
            --application-names "$APP_NAME" \
            --query "Applications[0].ApplicationName" \
            --output text >/dev/null 2>&1; then

            aws elasticbeanstalk create-application --application-name "$APP_NAME"
          fi

      - name: Create/Update EB environment
        run: |
          APP_NAME="Sample-AUTH-API"
          ENV_NAME="development"

          # Use latest .NET on Amazon Linux 2 platform ARN
          PLATFORM_ARN="arn:aws:elasticbeanstalk:${AWS_REGION}::platform/.NET Core on Linux 2/3.7.9"

          ENV_EXISTS=$(aws elasticbeanstalk describe-environments \
            --application-name "$APP_NAME" \
            --environment-names "$ENV_NAME" \
            --query "Environments[0].EnvironmentName" \
            --output text 2>/dev/null)

          if [ "$ENV_EXISTS" = "None" ] || [ -z "$ENV_EXISTS" ]; then
            echo "Creating new EB environment..."
            aws elasticbeanstalk create-environment \
              --application-name "$APP_NAME" \
              --environment-name "$ENV_NAME" \
              --platform-arn "$PLATFORM_ARN" \
              --version-label "$VERSION_LABEL"
          else
            echo "Updating EB environment..."
            aws elasticbeanstalk update-environment \
              --environment-name "$ENV_NAME" \
              --version-label "$VERSION_LABEL"
          fi
